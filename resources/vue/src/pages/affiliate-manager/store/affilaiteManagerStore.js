import { defineStore } from 'pinia';

export const useAffilaiteManagerStore = defineStore('affilaites-pages-affilaite-manager', {
    state: () => ({
        
    }),
    actions: {
        
    },
});
